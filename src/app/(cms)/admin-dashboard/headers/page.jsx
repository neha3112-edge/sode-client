import React from "react";
import HeaderCmsIndex from "@/features/headers";

export const metadata = {
  title: "Header Navigation | SODE Control Center",
  description: "Manage top header menu links and navigation items",
};

export default function HeadersPage() {
  return <HeaderCmsIndex />;
}
